import styles from "./notificationItem.module.scss"

const NotificationItem = () => {
  return (
    <div className={styles.alertItemContainer}>NotificationItem</div>
  )
}

export default NotificationItem